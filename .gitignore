# Repo-specific ignores (not the global gitignore — that's config/git/.gitignore_global)

.DS_Store
**/.stoobz/

# Guardrails — private blocked patterns (not tracked)
.guardrails

# Generated from .gitconfig.template by install script
config/git/.gitconfig
